<h1>Welcome to my Travel Site</h1>
<p>Stay tuned for cool travel stuff!</p>

<div id="map-canvas"></div>

<%= @homestate %>

<ul>
<% @countries.each do |country| %>
	<li><%= country %></li>
<% end %>
</ul>

<% @images.each do |pic| %>
	<%= image_tag pic, class: "img-responsive" %>
<% end %>


</table>
<table class="table">
  <tr>
    <% @united_states.each do |key,value| %>
	  <th><%= key.titleize %></th>
    <% end %>
  </tr>
  <tr>
    <% @united_states.each do |key,value| %>      
      <% if value.kind_of?(Array) %>
          <td><%= value.join(", ") %></td>
      <% else %>
         <td><%= value %></td>
      <% end %> 
    <% end %>
  </tr>
</table>

